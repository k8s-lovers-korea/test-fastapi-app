apiVersion: v1
kind: Service
metadata:
  name: test-fastapi-app
  labels:
    app: test-fastapi-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: test-fastapi-app